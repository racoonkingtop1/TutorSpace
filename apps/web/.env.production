# Loaded automatically by `vite build` (mode=production). This is what makes
# the GitHub Pages build self-contained: no VITE_API_URL is reachable there
# (no Express server on GitHub Pages), so the app runs entirely against the
# bundled demo dataset instead. See src/demo/mockServer.ts.
VITE_DEMO_MODE=true
